package udemy.section9;

public class Boolean {
    /*
    - 논리적 연산자
        boolean isValue = true ;
        boolean isValue = false;
        int i = 7 ;
        i > 7; // false
        i <= 7; //true
        int i = 17;
        if (15 >=i && i >= 25);
        && and; || or; ^ xor; ! not; | ; & ;
        &&; ||; 단축회로 연산자 (둘다 참이어야 출력, 수식 자체가 거짓이면 첫번째 조건이 거짓이면, 두번째 수식을 실행하지 않음)
        &; |; 회로 연산자 (수식의 모든 조건을 확인, 첫번째가 거짓이여도 두번째 수식을 실행)

        잘못된 예 (복잡)
        i > 15 && j++ >5
        >> 좋은 예
        j++
        i >15 && j > 5
     */
}
