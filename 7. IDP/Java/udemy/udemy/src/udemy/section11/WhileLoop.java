package udemy.section11;

public class WhileLoop {
    /*
        if 조건과 비슷
        int i = 0;
        if (i> 2) {
            System.out.println("Do");
            } 실행 X
        int i = 3;
        if (i> 2) {
            System.out.println("Do");
            } 실행 O
       int i = 0;
       if는 한번만 실행되지 만 while은 조건에 만족할 때 까지 실행된다.
       while (i <5) {
            System.out.println(i);
            i++; // i 값을 증가시키지 않으면 무한 루프에 걸린다.
        }
        output:
        0
        1
        2
        3
        4
     */
}
