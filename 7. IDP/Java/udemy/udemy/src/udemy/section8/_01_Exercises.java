package udemy.section8;

public class _01_Exercises {
    /*
    1. online shopping system
    - item
        . Status(Register, Holding)
        . Item(Quantity, price, name, vendor)
    - order
        . Status(Pending, Complete, reject)
        . PaymentMethod (bank, creditcard, ID)
    - delivery
        . status(intransit, prepare, completed)
        . Deliver(address, vendor, trackingNo)
    - actions
        . create(createID,registerTtem,payItem,deliveryStart,makeOrder)
        . modify(changeID,changeDeliveryAddress,changeOrder)
        . delete(deleteID,cancelOrder,rejectPayment,itemShort,deliceryFail)
    > Customer
        name, address
        login(), logout(), selectProduct(product)
    > Shopping Cart
        items
        add(item), remove(item)
    > Product
        name, price, quantityAvailable
        order(), changePrice()
    2. persion
    - gender
        . male
        . female
    - name
        . lastname
        . middlename
        . firstname
    - physical
        . height
        . weight
    - status
        . adult
        . live/dead
    - actions
        . emotions (crying, laughting, angry)
        . moves (sit/stand/down/work/sleep)
    > name, address, hobbies, work
        walk(), run(), sleep(), eat(), drink()
     */
}
